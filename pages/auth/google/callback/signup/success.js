// import { useRouter } from 'next/router';
import PageLoader from '../../../../../components/PageLoader';
// import { getHashKeyValPairFromUrl } from '../../../../../utils/parseHash';

const Success = () => {
    // const router = useRouter();
    // const { token, message } = getHashKeyValPairFromUrl(router.asPath);
    return <PageLoader />;
};

export default Success;
